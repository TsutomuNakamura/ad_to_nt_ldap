version: '3'

services:
  ruby:
    container_name: ad-ruby
    hostname: ruby
    build:
      context: ./dockerfile
      dockerfile: Dockerfile
    depends_on:
      - nt
    entrypoint:
      - tail
      - -f
      - /dev/null
    volumes:
      - .:/opt/app
    privileged: true

  nt:
    container_name: nt
    hostname: nt
    image: tsutomu/docker-samba-nt
    privileged: true

